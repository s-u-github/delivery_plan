<% provide(:title, "トップページ") %>

<div class="container" meta name="viewport" content="width=device-width,initial-scale=1">
  <div class="row">
    <!--<div class="col-xs-6 col-sm-4 col-md-3">-->
      <%= link_to "顧客リスト", user_articles_path(current_user.id), class: "btn btn-success top-button" %>
    <!--</div>-->
    <!--<div class="col-xs-6 col-sm-4 col-md-3">-->
      <%= link_to "配送計画作成", delivery_plan_user_articles_path(current_user.id), class: "btn btn-primary top-button" %>
    <!--</div>-->
    <!--<div class="col-xs-6 col-sm-4 col-md-3">-->
      <%= link_to "配送ルート", plan_list_user_articles_path(current_user.id, first_day: Date.today), class: "btn btn-info top-button" %>
    <!--</div>-->
    <!--<div class="col-xs-6 col-sm-4 col-md-3">-->
      <%= link_to "日報", daily_index_path(current_user.id, first_day: Date.today), class: "btn btn-danger top-button" %>
    <!--</div>-->
  </div>
  <!-- 画像 -->
  <div class="top-image">
    <%= image_tag 'top.jpg' %>
  </div>

</div>