<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="X-UA-Compatible" content="IE=10">
    
    <title><%= yield(:title) %></title>

    <%= csrf_meta_tags %>
    
    <%= include_gon %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= stylesheet_link_tag 'validationEngine.jquery.css' %> <!-- validationEngine.jquery.css　読み込み -->
    <%= javascript_include_tag 'jquery-3.4.1.min.js' %> <!-- jquery本体 読み込み -->
    <%= javascript_include_tag 'jquery.validationEngine.js' %> <!-- validationEngine本体 読み込み -->
    <%= javascript_include_tag 'jquery.validationEngine-ja.js' %> <!-- 日本語ファイル 読み込み -->
    <%= javascript_include_tag 'jquery.jpostal.js' %> <!-- jquery.jpostal.js 読み込み -->
    
    <%= render 'layouts/shim' %>
    
    <style type="text/css">
      html { height: 100% }
      body { height: 100% }
    </style>
  </head>

  <body>
    <%= render 'layouts/header' %>
    <div class="container">
      <% flash.each do |message_type, message| %>  <!-- flashというオブジェクトを対象にflashハッシュキーとバリュー(keyとvalue)を繰り返し処理をする -->
        <div class="alert alert-<%= message_type %>"><%= message %></div>  <!-- alert- ~ のところはCSSのクラスが動的に変化する。例 alert-sucucess -->
      <% end %>
      <%= yield %>
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
